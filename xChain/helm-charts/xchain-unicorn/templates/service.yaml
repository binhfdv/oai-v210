apiVersion: v1
kind: Service
metadata:
  name: {{ include "xchain-unicorn.fullname" . }}
  labels:
    {{- include "xchain-unicorn.labels" . | nindent 4 }}
spec:
  type: {{ .Values.serviceType | default "ClusterIP" }}
  ports:
    {{- range .Values.service }}
    - name: {{ .name }}
      port: {{ .containerPort }}
      targetPort: {{ .containerPort }}
      protocol: TCP
    {{- end }}
    {{- if .Values.agent.enabled }}
    - name: agent-http
      port: {{ .Values.agent.env.AGENT_PORT | default "5101" }}
      targetPort: {{ .Values.agent.env.AGENT_PORT | default "5101" }}
      protocol: TCP
    {{- end }}
  selector:
    {{- include "xchain-unicorn.selectorLabels" . | nindent 4 }}
