apiVersion: v1
kind: Service
metadata:
  name: {{ include "xchain-smartgw.fullname" . }}
  labels:
    {{- include "xchain-smartgw.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "xchain-smartgw.selectorLabels" . | nindent 4 }}
