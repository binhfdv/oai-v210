apiVersion: v1
kind: Service
metadata:
  name: {{ include "xchain-kcl.fullname" . }}
  labels:
    {{- include "xchain-kcl.labels" . | nindent 4 }}
spec:
  type: {{ .Values.serviceType | default "ClusterIP" }}
  ports:
    {{- range .Values.service }}
    - name: {{ .name }}
      port: {{ .containerPort }}
      targetPort: {{ .containerPort }}
      protocol: TCP
    {{- end }}
  selector:
    {{- include "xchain-kcl.selectorLabels" . | nindent 4 }}
