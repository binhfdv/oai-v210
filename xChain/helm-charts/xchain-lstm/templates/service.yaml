apiVersion: v1
kind: Service
metadata:
  name: {{ include "xchain-lstm.fullname" . }}
  labels:
    {{- include "xchain-lstm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.serviceType | default "ClusterIP" }}
  ports:
    {{- range .Values.service }}
    - name: {{ .name }}
      port: {{ .containerPort }}
      targetPort: {{ .containerPort }}
      protocol: TCP
    {{- end }}
  selector:
    {{- include "xchain-lstm.selectorLabels" . | nindent 4 }}
