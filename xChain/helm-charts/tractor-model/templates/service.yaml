apiVersion: v1
kind: Service
metadata:
  name: {{ include "tractor-model.fullname" . }}
  labels:
    {{- include "tractor-model.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tractor-model.selectorLabels" . | nindent 4 }}
