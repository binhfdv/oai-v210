apiVersion: v1
kind: Service
metadata:
  name: {{ include "xchain-smartgw-demo.fullname" . }}
  labels:
    {{- include "xchain-smartgw-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.serviceType | default "ClusterIP" }}
  ports:
    {{- range .Values.service }}
    - name: {{ .name }}
      port: {{ .containerPort }}
      targetPort: {{ .containerPort }}
      protocol: TCP
    {{- end }}
  selector:
    {{- include "xchain-smartgw-demo.selectorLabels" . | nindent 4 }}
