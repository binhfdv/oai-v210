apiVersion: v1
kind: Service
metadata:
  name: {{ include "xchain-gnn.fullname" . }}
  labels:
    {{- include "xchain-gnn.labels" . | nindent 4 }}
spec:
  type: {{ .Values.serviceType | default "ClusterIP" }}
  ports:
    {{- range .Values.service }}
    - name: {{ .name }}
      port: {{ .containerPort }}
      targetPort: {{ .containerPort }}
      protocol: TCP
    {{- end }}
  selector:
    {{- include "xchain-gnn.selectorLabels" . | nindent 4 }}
