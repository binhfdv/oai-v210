apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tractor-model.fullname" . }}-test-connection"
  labels:
    {{- include "tractor-model.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tractor-model.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
