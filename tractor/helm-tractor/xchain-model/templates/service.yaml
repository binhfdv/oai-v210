apiVersion: v1
kind: Service
metadata:
  name: {{ include "xchain-model.fullname" . }}
  labels:
    {{- include "xchain-model.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "xchain-model.selectorLabels" . | nindent 4 }}
