apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tractor-kpm-simu.fullname" . }}-test-connection"
  labels:
    {{- include "tractor-kpm-simu.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tractor-kpm-simu.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
