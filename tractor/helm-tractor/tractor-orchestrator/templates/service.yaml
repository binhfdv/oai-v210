apiVersion: v1
kind: Service
metadata:
  name: {{ include "tractor-orchestrator.fullname" . }}
spec:
  selector:
    app: {{ include "tractor-orchestrator.name" . }}
  ports:
    - port: 4200
      targetPort: 4200
      protocol: TCP
