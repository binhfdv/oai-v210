apiVersion: v1
kind: Service
metadata:
  name: {{ include "tractor-buffer.fullname" . }}
  labels:
    {{- include "tractor-buffer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tractor-buffer.selectorLabels" . | nindent 4 }}
