apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tractor-buffer.fullname" . }}-test-connection"
  labels:
    {{- include "tractor-buffer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tractor-buffer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
