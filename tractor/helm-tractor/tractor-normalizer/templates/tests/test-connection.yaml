apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tractor-normalizer.fullname" . }}-test-connection"
  labels:
    {{- include "tractor-normalizer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tractor-normalizer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
