apiVersion: v1
kind: Service
metadata:
  name: {{ include "tractor-normalizer.fullname" . }}
  labels:
    {{- include "tractor-normalizer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tractor-normalizer.selectorLabels" . | nindent 4 }}
