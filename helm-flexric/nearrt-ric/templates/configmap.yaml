apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-configmap
data:
  flexric.conf: |
    [NEAR-RIC]
    NEAR_RIC_IP = {{ .Values.multus.e2Interface.ipAdd }}
    
    [XAPP]
    DB_DIR = /tmp/
